export const CAPABILITIES = ["USE", "MANAGE", "GRANT", "DELEGATE"];

export const CAPABILITY_KEYS = ["use", "manage", "grant", "delegate"];

export const RBAC_SCOPES = ["global", "role", "user"];

export const GRANT_DEFAULT_CAPABILITIES = {
  use: true,
  manage: false,
  grant: false,
  delegate: false,
};